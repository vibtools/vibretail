# Code Style

- PHP: PHP 8.1+ compatible, `declare(strict_types=1)` for maintained PHP entrypoints where practical.
- JavaScript/CSS: preserve existing behavior until a dedicated UI/UX/refactor scope is approved.
- New repository utilities belong in `scripts/`; automated verification belongs in `tests/`.
- Do not mix internal project records into public `docs/`.
