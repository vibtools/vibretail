# Environment Configuration

Fresh installations are intended to use the guarded web installer, which creates the runtime environment configuration automatically. Active environment files contain secrets and must never be committed to Git.
